@model IEnumerable<PetApp_Empresa.Models.CarritoDeCompra>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.UsuarioId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarritoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarritoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarritoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
