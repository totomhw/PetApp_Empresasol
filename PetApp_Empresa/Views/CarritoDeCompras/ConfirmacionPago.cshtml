<div class="container py-5">
    <!-- Título -->
    <div class="text-center mb-4 animate-title">
        <h1 class="display-4 fw-bold title-gradient">Gracias por su compra</h1>
        <p class="lead text-muted animate-subtitle">
            Su pago ha sido procesado con éxito. Nos pondremos en contacto para coordinar el envío.
        </p>
    </div>

    <!-- Mensaje principal -->
    <div class="border-gradient mx-auto p-4 text-center animate-content">
        <p class="fs-5 text-white animate-text">
            Si no recibes una respuesta a la brevedad, por favor envía un mensaje al
            <span class="text-highlight">72164449</span> adjuntando tu comprobante de pago.
        </p>
        <a asp-action="ListadoAccesorios" asp-controller="Accesorios" class="btn btn-gradient mt-4 animate-button">
            <i class="fas fa-store me-2"></i>Volver a la Tienda
        </a>
    </div>
</div>

<!-- Estilos -->
<style>
    body {
        font-family: 'Poppins', sans-serif;
        background-color: #1a1a1a;
        color: #ffffff;
    }

    /* Título gradiente */
    .title-gradient {
        background: linear-gradient(to right, #800020, #a81c1c); /* Rojo guindo */
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: glowingTitle 1.5s infinite alternate;
    }

    @@keyframes glowingTitle {
        from {
            filter: brightness(1);
        }

        to {
            filter: brightness(1.5);
        }
    }

    /* Contenedor con borde gradiente */
    .border-gradient {
        position: relative;
        max-width: 600px;
        background: #800020; /* Rojo guindo */
        border-radius: 12px;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        overflow: hidden;
    }

        .border-gradient::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: 4px solid transparent;
            border-radius: 12px;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: glowEffect 5s infinite; /* Animación completa cada 7 segundos */
            z-index: 1;
            pointer-events: none;
        }

    @@keyframes glowEffect {
        0% {
            transform: translateX(-100%);
        }

        57.14% {
            transform: translateX(100%);
        }

        100% {
            transform: translateX(100%);
        }
    }

    /* Botón gradiente */
    .btn-gradient {
        background: linear-gradient(to right, #800020, #a81c1c); /* Rojo guindo */
        color: #ffffff !important; /* Texto blanco */
        border: none;
        font-weight: bold;
        border-radius: 8px;
        padding: 10px 20px;
        font-size: 1.2rem;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

        .btn-gradient:hover {
            transform: scale(1.1);
            box-shadow: 0px 8px 20px rgba(168, 28, 28, 0.5);
        }

    /* Texto destacado */
    .text-highlight {
        color: #ffcc00;
        font-weight: bold;
    }

    /* Animaciones */
    .animate-title {
        opacity: 0;
        transform: translateY(-20px);
        animation: fadeIn 1s forwards ease-out;
    }

    .animate-subtitle {
        opacity: 0;
        transform: translateY(-10px);
        animation: fadeIn 1s 0.5s forwards ease-out;
    }

    .animate-content {
        opacity: 0;
        transform: translateY(30px);
        animation: fadeIn 1s 0.7s forwards ease-out;
    }

    .animate-text {
        opacity: 0;
        transform: translateY(10px);
        animation: fadeIn 1s 0.9s forwards ease-out;
    }

    .animate-button {
        opacity: 0;
        transform: scale(0.9);
        animation: fadeInScale 1s 1.2s forwards ease-out;
    }

    @@keyframes fadeIn {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@keyframes fadeInScale {
        to {
            opacity: 1;
            transform: scale(1);
        }
    }
</style>

<!-- Librerías necesarias -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
